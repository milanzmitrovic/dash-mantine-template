"""
Purpose of this file is to organize
logging of application.

There should be created function
for each log level.

So that, in future, if there is
need to change underlying logging
library, we can easily switch to
new one (by changing config at
central module location).
"""
